<UserControl x:Class="Caveability_mvvm.Views.CaveabilityView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Caveability_mvvm.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0">
            <Button Content = "Footwall" Command="{Binding ChangeCurrentView}" CommandParameter="Footwall" HorizontalAlignment="Stretch" Height = "50"/>
            <Button Content = "Hangwall" Command="{Binding ChangeCurrentView}" CommandParameter="Hangwall" HorizontalAlignment="Stretch" Height = "50"/>
            <Button Content = "StopeBack" Command="{Binding ChangeCurrentView}" CommandParameter="StopeBack" HorizontalAlignment="Stretch" Height = "50"/>
            <Button Content = "StopeFront" Command="{Binding ChangeCurrentView}" CommandParameter="StopeFront" HorizontalAlignment="Stretch" Height = "50"/>
        </StackPanel>
    </Grid>
</UserControl>
